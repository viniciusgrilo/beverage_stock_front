Installation Guide:

- Make sure the 'beverage-stock-api' is up and running. The API is avaliable at 'https://github.com/viniciusgrilo/beverage_stock_api'.
- Install libraries using 'pip install -r requirements.txt'.
- Run 'streamlit run home.py'.